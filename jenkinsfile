pipeline {
    agent any

    stages {
        stage('Checkout Automation Repo') {
            steps {
                checkout scm   // pulls Repo B (automation)
            }
        }

        stage('Run Automation') {
            steps {
                sh "mvn clean test"
            }
        }
    }
}
